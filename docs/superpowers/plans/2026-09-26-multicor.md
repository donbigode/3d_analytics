# Multicor — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Um item de orçamento passa a ter N filamentos, cada um com seu material e suas gramas, compartilhando um único tempo de impressão e uma única quantidade.

**Architecture:** Tabela nova `quote_item_filaments` é autoritativa; `quote_items.material_version_id` continua existindo como derivado da linha 1 (expand/contract), para que o repositório `3d_analytics_medallion` siga funcionando sem mudança. Gramas somam por linha; energia e depreciação contam o `time_s` uma única vez por item, multiplicado pela quantidade.

**Tech Stack:** FastAPI, SQLAlchemy 2 async, Alembic, PostgreSQL 16, Pydantic v2, pytest/pytest-asyncio, Jinja2 + WeasyPrint (PDF), SvelteKit 2 / Svelte 5, Vitest.

**Spec:** `docs/superpowers/specs/2026-09-26-multicor-design.md`

## Global Constraints

- **Tudo roda dentro do container.** `docker compose run --rm api pytest …`, `… ruff check …`, `… alembic upgrade head`. `pytest`/`python` no shell puro falham.
- **Lint:** a base do projeto tem ~920 erros pré-existentes com `ruff check backend`. O gate é **só os arquivos alterados**, e só o conjunto default: `ruff check --select E4,E7,E9,F <arquivos>`. Zero erro novo nesse conjunto.
- **Gramas são sempre "por peça".** `grams_unit`, `effective_grams_per_unit`, `filament_m`, `filament_g` — todos por peça. A multiplicação por `quantity` acontece num lugar só, no cálculo de custo.
- **`time_s` é por peça e único por item.** Nunca somar por linha de filamento. Multiplicar por `quantity`, uma vez.
- **Dinheiro não se arredonda por linha.** Somar sem arredondar e arredondar só o agregado (padrão `_q2` em `dre.py`/`facts.py`). Ver o comentário em `_consumptions_map`.
- **Diâmetro do filamento:** `Decimal("1.75")` é o default em toda parte.
- **Mensagens de erro de API em português**, como as existentes (`"informe a data da venda"`, `"item '<nome>' has no filament length…"` — as antigas em inglês ficam como estão; as novas são em português).
- **Migrações:** `revision` é o nome do arquivo sem `.py`; `down_revision` aponta para a anterior. A última existente é `0033_sales_quote_kind_backfill`.

## Nota sobre testar backfill neste projeto

`backend/tests/conftest.py` roda `alembic upgrade head` **uma vez por sessão**, antes de qualquer dado de teste existir. Como diz o docstring de `backend/tests/api/test_quote_seq.py`:

> *"A suíte roda as migrações contra o banco de teste, então o backfill já aconteceu quando estes testes rodam — o que eles verificam é o comportamento da coluna daí em diante."*

Consequência prática: **não é possível** escrever "crie dados antigos → rode a migração → confira" na suíte normal. A spec descreve o teste de backfill nesses termos; este plano o realiza de outra forma.

A solução usada nas Tasks 1 e 2: o SQL do backfill vive numa constante módulo-level da migração, e o teste importa essa constante e a executa contra linhas que ele mesmo criou pelo ORM. O mesmo SQL que roda em produção roda no teste — o que é o ponto — sem depender da ordem de migração.

## Estrutura de arquivos

**Criar**
| arquivo | responsabilidade |
|---|---|
| `migrations/versions/0034_quote_item_filaments.py` | tabela `quote_item_filaments`, coluna `material_consumptions.quote_item_filament_id`, backfill estrutural |
| `migrations/versions/0035_spools_material_version.py` | `spools.material_version_id` + backfill por versão corrente |
| `backend/infra/db/models/quote_item_filament.py` | modelo `QuoteItemFilament` |
| `backend/core/quotes/filaments.py` | regras puras do conjunto de linhas: refugo por linha, gramas por linha, validação das invariantes. Sem I/O. |
| `backend/tests/core/test_filaments_rules.py` | testes das regras puras |
| `backend/tests/core/test_migracao_0034_backfill.py` | backfill estrutural + custo antes/depois |
| `backend/tests/core/test_migracao_0035_spools.py` | casamento SCD2, ambiguidade, idempotência |
| `backend/tests/api/test_item_filaments_api.py` | contrato de API e invariantes via HTTP |
| `backend/tests/api/test_produce_multicor.py` | produção multicor, 409, estoque intacto |
| `frontend/src/lib/components/FilamentLines.svelte` | editor das linhas de um item |
| `frontend/src/lib/filament-lines.ts` | lógica pura do editor (soma, delta, renumeração) |
| `frontend/src/lib/filament-lines.test.ts` | testes Vitest da lógica pura |

**Modificar**
| arquivo | o que |
|---|---|
| `backend/infra/db/models/__init__.py` | registrar `QuoteItemFilament` |
| `backend/infra/db/models/material_consumption.py` | `quote_item_filament_id` |
| `backend/infra/db/models/spool.py` | `material_version_id` |
| `backend/core/pricing/quote.py` | `ItemInput.filaments` substitui `grams`+`price_per_kg`; `compute_item_cost` soma as linhas |
| `backend/core/quote_service.py` | `gcode_to_item_input` monta `filaments` |
| `backend/api/routes/quotes/_shared.py` | `_build_item_input` lê as linhas; `_quote_out` expõe `filaments`; `_assert_materials_resolved` olha linhas |
| `backend/core/accounting/cost.py` | CPV soma as linhas |
| `backend/api/schemas/quotes.py` | `QuoteItemFilamentIn/Out`, `QuoteItemOut.filaments`, `QuoteItemUpdate.filaments`, `ConsumptionAssignment.quote_item_filament_id`, `ConsumptionOut.filament_id` |
| `backend/api/routes/quotes/items.py` | `add_item` cria a linha 1; `update_item` substitui a lista |
| `backend/api/routes/quotes/transitions.py` | `apply_production`: densidade da linha, sem fallback de item inteiro, 409 |
| `backend/infra/pdf/templates/quote.html` | macro `material_label` lista as linhas |
| `frontend/src/routes/quotes/[id]/+page.svelte` | usar `FilamentLines`, enviar `filaments` no PATCH |
| `docs/superpowers/specs/2026-09-26-multicor-design.md` | corrigir a forma do teste de backfill (seção 10) |

---

## Task 1: Modelo e migração 0034

**Files:**
- Create: `backend/infra/db/models/quote_item_filament.py`
- Create: `migrations/versions/0034_quote_item_filaments.py`
- Create: `backend/tests/core/test_migracao_0034_backfill.py`
- Modify: `backend/infra/db/models/__init__.py`
- Modify: `backend/infra/db/models/material_consumption.py`

**Interfaces:**
- Consumes: nada.
- Produces:
  - `QuoteItemFilament` com atributos `id: UUID`, `quote_item_id: UUID`, `material_version_id: UUID`, `grams_unit: Decimal | None`, `position: int`
  - `MaterialConsumption.quote_item_filament_id: UUID | None`
  - `migrations.versions.0034_quote_item_filaments.BACKFILL_SQL: str` — o `INSERT` idempotente, importável por testes

- [ ] **Step 1: Escrever o teste do backfill (vai falhar: não há tabela)**

Criar `backend/tests/core/test_migracao_0034_backfill.py`:

```python
"""Backfill estrutural da 0034.

conftest.py roda `alembic upgrade head` antes de qualquer dado de teste
existir, então não dá para criar dados "antigos" e migrar em cima. O SQL do
backfill vive numa constante da migração e é exercido aqui contra itens
criados pelo ORM — o mesmo SQL que roda em produção.
"""
import importlib
from decimal import Decimal

import pytest
import sqlalchemy as sa

from backend.core.models import QuoteKind, QuoteStatus
from backend.infra.db import session as session_module
from backend.infra.db.models import (
    MaterialVersion, Quote, QuoteItem, QuoteItemFilament, User,
)

_mig = importlib.import_module("migrations.versions.0034_quote_item_filaments")


async def _item_sem_linha(grams_meta: dict) -> tuple[QuoteItem, MaterialVersion]:
    """Cria um QuoteItem direto pelo ORM, sem passar pela rota — que a partir
    da Task 6 criaria a linha 1 automaticamente. É assim que se simula um item
    anterior à migração."""
    async with session_module.SessionFactory() as s:
        u = (await s.execute(sa.select(User))).scalars().first()
        mv = MaterialVersion(
            material_type="PLA", name="PLA Preto", color="Preto",
            manufacturer="ACME", density_g_cm3=Decimal("1.24"),
            price_per_kg_ref=Decimal("100"),
        )
        s.add(mv)
        await s.commit()
        q = Quote(kind=QuoteKind.COMMERCIAL.value, user_id=u.id,
                  status=QuoteStatus.DRAFT.value, markup_pct=Decimal("0"),
                  min_charge=Decimal("0"))
        s.add(q)
        await s.commit()
        it = QuoteItem(quote_id=q.id, name="peça", gcode_meta=grams_meta,
                       material_version_id=mv.id, quantity=2)
        s.add(it)
        await s.commit()
        await s.refresh(it)
        await s.refresh(mv)
        return it, mv


@pytest.mark.asyncio
async def test_backfill_cria_uma_linha_com_gramas_nulas():
    it, mv = await _item_sem_linha({"filament_m": 10, "time_s": 3600})
    async with session_module.SessionFactory() as s:
        await s.execute(sa.text(_mig.BACKFILL_SQL))
        await s.commit()
        linhas = (await s.execute(
            sa.select(QuoteItemFilament)
              .where(QuoteItemFilament.quote_item_id == it.id)
        )).scalars().all()

    assert len(linhas) == 1
    assert linhas[0].position == 1
    assert linhas[0].material_version_id == mv.id
    assert linhas[0].grams_unit is None, (
        "o backfill não deve congelar gramas — NULL significa 'derive do gcode_meta'"
    )


@pytest.mark.asyncio
async def test_backfill_e_idempotente():
    it, _ = await _item_sem_linha({"filament_m": 10, "time_s": 3600})
    async with session_module.SessionFactory() as s:
        await s.execute(sa.text(_mig.BACKFILL_SQL))
        await s.execute(sa.text(_mig.BACKFILL_SQL))
        await s.commit()
        n = (await s.execute(
            sa.select(sa.func.count()).select_from(QuoteItemFilament)
              .where(QuoteItemFilament.quote_item_id == it.id)
        )).scalar_one()
    assert n == 1, "rodar o backfill duas vezes duplicou a linha"


@pytest.mark.asyncio
async def test_item_sem_material_fica_sem_linha_e_sem_erro():
    async with session_module.SessionFactory() as s:
        u = (await s.execute(sa.select(User))).scalars().first()
        q = Quote(kind=QuoteKind.COMMERCIAL.value, user_id=u.id,
                  status=QuoteStatus.DRAFT.value, markup_pct=Decimal("0"),
                  min_charge=Decimal("0"))
        s.add(q)
        await s.commit()
        it = QuoteItem(quote_id=q.id, name="pendente",
                       gcode_meta={"filament_m": 5, "time_s": 600},
                       material_version_id=None, quantity=1)
        s.add(it)
        await s.commit()
        await s.refresh(it)

        await s.execute(sa.text(_mig.BACKFILL_SQL))   # não deve levantar
        await s.commit()
        n = (await s.execute(
            sa.select(sa.func.count()).select_from(QuoteItemFilament)
              .where(QuoteItemFilament.quote_item_id == it.id)
        )).scalar_one()
    assert n == 0
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker compose run --rm api pytest backend/tests/core/test_migracao_0034_backfill.py -q
```

Esperado: erro de importação — `QuoteItemFilament` não existe em `backend.infra.db.models`, e o módulo da migração não existe.

- [ ] **Step 3: Criar o modelo**

`backend/infra/db/models/quote_item_filament.py`:

```python
from decimal import Decimal
from uuid import UUID, uuid4

from sqlalchemy import ForeignKey, Numeric, UniqueConstraint
from sqlalchemy.dialects.postgresql import UUID as PG_UUID
from sqlalchemy.orm import Mapped, mapped_column

from backend.infra.db.base import Base


class QuoteItemFilament(Base):
    """Um filamento usado por um item — cor, material e gramas.

    Autoritativa para custo. ``quote_items.material_version_id`` é o derivado
    desta tabela: sempre igual ao material da linha de ``position = 1``.

    ``grams_unit`` é **por peça**, e é NULL de propósito para todo item que
    existia antes da migração 0034: NULL significa "derive do ``gcode_meta``
    como sempre". Congelar o valor calculado no backfill faria o histórico
    parar de reprecificar quando a densidade do material fosse ajustada.

    Item com mais de uma linha exige ``grams_unit`` em **todas** — não se
    deriva do gcode o que é ambíguo entre N cores.
    """

    __tablename__ = "quote_item_filaments"
    __table_args__ = (
        UniqueConstraint("quote_item_id", "position", name="uq_quote_item_filament_pos"),
    )

    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid4)
    quote_item_id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True),
        ForeignKey("quote_items.id", ondelete="CASCADE"),
        nullable=False,
        index=True,
    )
    material_version_id: Mapped[UUID] = mapped_column(
        PG_UUID(as_uuid=True), ForeignKey("material_versions.id"), nullable=False
    )
    grams_unit: Mapped[Decimal | None] = mapped_column(Numeric(10, 2))
    position: Mapped[int] = mapped_column(nullable=False)
```

Conferir o caminho real do `Base` antes de escrever o import — os outros modelos em `backend/infra/db/models/` mostram qual é.

- [ ] **Step 4: Registrar no `__init__.py`**

Em `backend/infra/db/models/__init__.py`, adicionar o import junto aos demais e o nome em `__all__`:

```python
from backend.infra.db.models.quote_item_filament import QuoteItemFilament
```

e incluir `"QuoteItemFilament"` na lista `__all__`, ao lado de `"QuoteItem"`.

- [ ] **Step 5: Adicionar a coluna em `MaterialConsumption`**

Em `backend/infra/db/models/material_consumption.py`, depois de `quote_item_id`:

```python
    # Qual linha de filamento originou este consumo. NULL = consumo anterior à
    # 0034. Sem isto, várias linhas de consumo no mesmo item são ambíguas entre
    # "duas cores" e "duas tentativas" (reimpressão) — e a segunda já existia.
    # Não dá para inferir por spool_id (duas cores podem sair do mesmo produto)
    # nem por consumed_at (heurística sobre dinheiro).
    quote_item_filament_id: Mapped[UUID | None] = mapped_column(
        PG_UUID(as_uuid=True), ForeignKey("quote_item_filaments.id", ondelete="SET NULL")
    )
```

- [ ] **Step 6: Escrever a migração**

`migrations/versions/0034_quote_item_filaments.py`:

```python
"""quote_item_filaments + material_consumptions.quote_item_filament_id

Um item passa a ter N filamentos. A tabela nova é autoritativa para custo;
quote_items.material_version_id continua existindo como derivado da linha 1,
para que o repositório 3d_analytics_medallion siga funcionando sem mudança
(expand/contract).

O backfill é PURAMENTE ESTRUTURAL: grams_unit fica NULL, que significa
"derive do gcode_meta como hoje". Gravar as gramas calculadas congelaria um
valor derivado e o histórico pararia de reprecificar quando a densidade do
material fosse ajustada.

BACKFILL_SQL é módulo-level de propósito: conftest.py roda as migrações antes
de qualquer dado de teste existir, então o teste do backfill importa esta
constante e a executa contra dados próprios. É o mesmo SQL que roda aqui.

Revision ID: 0034_quote_item_filaments
Revises: 0033_sales_quote_kind_backfill
Create Date: 2026-09-26 12:00:00.000000
"""
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

revision: str = "0034_quote_item_filaments"
down_revision: Union[str, Sequence[str], None] = "0033_sales_quote_kind_backfill"
branch_labels = None
depends_on = None


# Idempotente pelo NOT EXISTS: rodar de novo não duplica.
# Itens sem material resolvido ficam sem linha — a invariante "todo item tem ao
# menos uma linha" vale a partir da resolução do material, que é o que
# _assert_materials_resolved já exige antes de produzir.
BACKFILL_SQL = """
INSERT INTO quote_item_filaments
       (id, quote_item_id, material_version_id, grams_unit, position)
SELECT gen_random_uuid(), qi.id, qi.material_version_id, NULL, 1
  FROM quote_items qi
 WHERE qi.material_version_id IS NOT NULL
   AND NOT EXISTS (
         SELECT 1 FROM quote_item_filaments f WHERE f.quote_item_id = qi.id
       )
"""


def upgrade() -> None:
    op.create_table(
        "quote_item_filaments",
        sa.Column("id", postgresql.UUID(as_uuid=True), primary_key=True),
        sa.Column(
            "quote_item_id",
            postgresql.UUID(as_uuid=True),
            sa.ForeignKey("quote_items.id", ondelete="CASCADE"),
            nullable=False,
        ),
        sa.Column(
            "material_version_id",
            postgresql.UUID(as_uuid=True),
            sa.ForeignKey("material_versions.id"),
            nullable=False,
        ),
        sa.Column("grams_unit", sa.Numeric(10, 2), nullable=True),
        sa.Column("position", sa.Integer(), nullable=False),
        sa.UniqueConstraint("quote_item_id", "position", name="uq_quote_item_filament_pos"),
    )
    op.create_index(
        "ix_quote_item_filaments_item", "quote_item_filaments", ["quote_item_id"]
    )

    op.add_column(
        "material_consumptions",
        sa.Column("quote_item_filament_id", postgresql.UUID(as_uuid=True), nullable=True),
    )
    op.create_foreign_key(
        "fk_material_consumptions_filament",
        "material_consumptions",
        "quote_item_filaments",
        ["quote_item_filament_id"],
        ["id"],
        ondelete="SET NULL",
    )

    op.execute(BACKFILL_SQL)


def downgrade() -> None:
    op.drop_constraint(
        "fk_material_consumptions_filament", "material_consumptions", type_="foreignkey"
    )
    op.drop_column("material_consumptions", "quote_item_filament_id")
    op.drop_index("ix_quote_item_filaments_item", table_name="quote_item_filaments")
    op.drop_table("quote_item_filaments")
```

`gen_random_uuid()` é nativo do Postgres 13+; a imagem é `pgvector/pgvector:pg16`.

- [ ] **Step 7: Rodar a migração e os testes**

```bash
docker compose run --rm api alembic upgrade head
docker compose run --rm api pytest backend/tests/core/test_migracao_0034_backfill.py -q
```

Esperado: migração aplica, 3 testes passam.

- [ ] **Step 8: Conferir que o downgrade volta limpo**

```bash
docker compose run --rm api alembic downgrade -1
docker compose run --rm api alembic upgrade head
```

Esperado: os dois sem erro. (Rodar `upgrade` de novo deixa o banco de teste no estado certo para os próximos passos.)

- [ ] **Step 9: Suíte inteira + lint**

```bash
docker compose run --rm api pytest backend/tests -q
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/infra/db/models/quote_item_filament.py \
  backend/infra/db/models/material_consumption.py \
  backend/infra/db/models/__init__.py \
  migrations/versions/0034_quote_item_filaments.py \
  backend/tests/core/test_migracao_0034_backfill.py
```

Esperado: 305 + 3 = **308 passed**; zero erro de lint nesse conjunto.

- [ ] **Step 10: Commit**

```bash
git add backend/infra/db/models/quote_item_filament.py \
        backend/infra/db/models/material_consumption.py \
        backend/infra/db/models/__init__.py \
        migrations/versions/0034_quote_item_filaments.py \
        backend/tests/core/test_migracao_0034_backfill.py
git commit -m "feat(multicor): tabela quote_item_filaments e vínculo do consumo

Backfill estrutural: grams_unit NULL significa 'derive do gcode_meta',
então o custo de todo orçamento histórico fica idêntico. BACKFILL_SQL é
módulo-level porque a suíte roda as migrações antes de haver dados."
```

---

## Task 2: Migração 0035 — `spools.material_version_id`

**Files:**
- Create: `migrations/versions/0035_spools_material_version.py`
- Create: `backend/tests/core/test_migracao_0035_spools.py`
- Modify: `backend/infra/db/models/spool.py`

**Interfaces:**
- Consumes: `0034_quote_item_filaments` como `down_revision`.
- Produces:
  - `Spool.material_version_id: UUID | None`
  - `migrations.versions.0035_spools_material_version.BACKFILL_SQL: str`

- [ ] **Step 1: Escrever o teste (vai falhar: coluna não existe)**

Criar `backend/tests/core/test_migracao_0035_spools.py`:

```python
"""Backfill de spools.material_version_id.

material_versions é SCD2 — várias linhas por (material_type, manufacturer,
color), uma marcada is_current. O vínculo certo para uma bobina é a versão
corrente, e só quando ela é única.

O vínculo é de IDENTIDADE, não de preço: o custo da bobina continua vindo do
seu próprio purchased_price / initial_grams.
"""
import importlib
from datetime import datetime, timezone
from decimal import Decimal

import pytest
import sqlalchemy as sa

from backend.infra.db import session as session_module
from backend.infra.db.models import MaterialVersion, Spool

_mig = importlib.import_module("migrations.versions.0035_spools_material_version")


async def _bobina(material_type: str, color: str, manufacturer: str) -> Spool:
    async with session_module.SessionFactory() as s:
        sp = Spool(
            material_type=material_type, color=color, manufacturer=manufacturer,
            purchased_at=datetime.now(timezone.utc), purchased_price=Decimal("100"),
            initial_grams=Decimal("1000"), remaining_grams=Decimal("1000"),
        )
        s.add(sp)
        await s.commit()
        await s.refresh(sp)
        return sp


async def _versao(material_type: str, color: str, manufacturer: str,
                  is_current: bool = True) -> MaterialVersion:
    async with session_module.SessionFactory() as s:
        mv = MaterialVersion(
            material_type=material_type, name=f"{material_type} {color}",
            color=color, manufacturer=manufacturer,
            density_g_cm3=Decimal("1.24"), price_per_kg_ref=Decimal("100"),
            is_current=is_current,
        )
        s.add(mv)
        await s.commit()
        await s.refresh(mv)
        return mv


async def _rodar_backfill() -> None:
    async with session_module.SessionFactory() as s:
        await s.execute(sa.text(_mig.BACKFILL_SQL))
        await s.commit()


async def _vinculo(spool_id) -> object:
    async with session_module.SessionFactory() as s:
        return (await s.execute(
            sa.select(Spool.material_version_id).where(Spool.id == spool_id)
        )).scalar_one()


@pytest.mark.asyncio
async def test_casa_com_a_versao_corrente_quando_unica():
    mv = await _versao("PETG", "Azul-035a", "FabX")
    sp = await _bobina("PETG", "Azul-035a", "FabX")
    await _rodar_backfill()
    assert await _vinculo(sp.id) == mv.id


@pytest.mark.asyncio
async def test_ignora_versao_nao_corrente():
    await _versao("ABS", "Cinza-035b", "FabY", is_current=False)
    sp = await _bobina("ABS", "Cinza-035b", "FabY")
    await _rodar_backfill()
    assert await _vinculo(sp.id) is None, (
        "versão fora de is_current não deve ser vinculada"
    )


@pytest.mark.asyncio
async def test_casamento_ambiguo_deixa_nulo():
    # Duas versões correntes para o mesmo trio — não deveria existir, mas o
    # esquema não impede. Escolher uma arbitrariamente é pior que não escolher.
    await _versao("PLA", "Verde-035c", "FabZ")
    await _versao("PLA", "Verde-035c", "FabZ")
    sp = await _bobina("PLA", "Verde-035c", "FabZ")
    await _rodar_backfill()
    assert await _vinculo(sp.id) is None


@pytest.mark.asyncio
async def test_sem_material_correspondente_deixa_nulo():
    sp = await _bobina("TPU", "Cor-que-ninguem-cadastrou-035d", "FabW")
    await _rodar_backfill()
    assert await _vinculo(sp.id) is None


@pytest.mark.asyncio
async def test_e_idempotente():
    mv = await _versao("PLA-CF", "Preto-035e", "FabV")
    sp = await _bobina("PLA-CF", "Preto-035e", "FabV")
    await _rodar_backfill()
    await _rodar_backfill()
    assert await _vinculo(sp.id) == mv.id
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker compose run --rm api pytest backend/tests/core/test_migracao_0035_spools.py -q
```

Esperado: `ModuleNotFoundError` para a migração, ou `AttributeError` em `Spool.material_version_id`.

- [ ] **Step 3: Adicionar a coluna no modelo**

Em `backend/infra/db/models/spool.py`, depois de `notes`:

```python
    # Qual produto (MaterialVersion) é esta bobina. Nullable: bobina cadastrada
    # antes do material, ou com a cor escrita diferente, não casa no backfill —
    # e inventar um vínculo errado é pior que deixar NULL.
    #
    # IDENTIDADE, NÃO PREÇO. O custo da bobina vem do seu próprio
    # purchased_price / initial_grams. Não derivar custo daqui via
    # price_per_kg_ref: seria trocar o preço pago por um preço de referência.
    material_version_id: Mapped[UUID | None] = mapped_column(
        PG_UUID(as_uuid=True), ForeignKey("material_versions.id")
    )
```

Conferir se `ForeignKey` e `PG_UUID` já estão importados no arquivo; se não, adicionar.

- [ ] **Step 4: Escrever a migração**

`migrations/versions/0035_spools_material_version.py`:

```python
"""spools.material_version_id

Existe para o repositório 3d_analytics_medallion poder atribuir consumo de
filamento pela bobina de onde ele saiu, em vez de pelo único material do item
— que com multicor joga as gramas de todas as cores no mesmo material.
Ver gold/consumo.py lá, cuja própria docstring declara a limitação.

material_versions é SCD2 (effective_from/effective_to/is_current), com várias
linhas por (material_type, manufacturer, color). O vínculo certo é a versão
CORRENTE, e só quando ela é única: duas linhas is_current para o mesmo trio
não deveriam existir, mas o esquema não impede, e escolher arbitrariamente
seria vincular a bobina ao produto errado.

Nullable de propósito. Taxa de casamento baixa não é falha da migração — é
sinal de que os cadastros divergem.

Revision ID: 0035_spools_material_version
Revises: 0034_quote_item_filaments
Create Date: 2026-09-26 12:30:00.000000
"""
from typing import Sequence, Union

import sqlalchemy as sa
from alembic import op
from sqlalchemy.dialects import postgresql

revision: str = "0035_spools_material_version"
down_revision: Union[str, Sequence[str], None] = "0034_quote_item_filaments"
branch_labels = None
depends_on = None


# min(id) está no SELECT só porque o GROUP BY exige uma agregação; ele nunca é
# usado quando n > 1, porque o WHERE descarta esses casos.
# IS NOT DISTINCT FROM em vez de = porque color e manufacturer são nullable.
BACKFILL_SQL = """
WITH candidato AS (
  SELECT material_type, manufacturer, color,
         min(id) AS id, count(*) AS n
    FROM material_versions
   WHERE is_current
   GROUP BY material_type, manufacturer, color
)
UPDATE spools s
   SET material_version_id = c.id
  FROM candidato c
 WHERE c.n = 1
   AND c.material_type = s.material_type
   AND c.color        IS NOT DISTINCT FROM s.color
   AND c.manufacturer IS NOT DISTINCT FROM s.manufacturer
   AND s.material_version_id IS NULL
"""


def upgrade() -> None:
    op.add_column(
        "spools",
        sa.Column("material_version_id", postgresql.UUID(as_uuid=True), nullable=True),
    )
    op.create_foreign_key(
        "fk_spools_material_version",
        "spools",
        "material_versions",
        ["material_version_id"],
        ["id"],
    )
    op.execute(BACKFILL_SQL)


def downgrade() -> None:
    op.drop_constraint("fk_spools_material_version", "spools", type_="foreignkey")
    op.drop_column("spools", "material_version_id")
```

- [ ] **Step 5: Rodar migração e testes**

```bash
docker compose run --rm api alembic upgrade head
docker compose run --rm api pytest backend/tests/core/test_migracao_0035_spools.py -q
```

Esperado: 5 testes passam.

- [ ] **Step 6: Downgrade e volta**

```bash
docker compose run --rm api alembic downgrade -1
docker compose run --rm api alembic upgrade head
```

- [ ] **Step 7: Suíte + lint**

```bash
docker compose run --rm api pytest backend/tests -q
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/infra/db/models/spool.py \
  migrations/versions/0035_spools_material_version.py \
  backend/tests/core/test_migracao_0035_spools.py
```

Esperado: **313 passed** (308 + 5).

- [ ] **Step 8: Commit**

```bash
git add backend/infra/db/models/spool.py \
        migrations/versions/0035_spools_material_version.py \
        backend/tests/core/test_migracao_0035_spools.py
git commit -m "feat(multicor): spools.material_version_id

Casa pela versão corrente (material_versions é SCD2) e só quando ela é
única. Existe para o medallion atribuir consumo pela bobina, não pelo
único material do item. Identidade, não preço."
```

---

## Task 3: Regras puras das linhas — `backend/core/quotes/filaments.py`

Módulo sem I/O, para que as regras de refugo e as invariantes sejam testáveis sem banco e tenham **um** lugar só. Sem ele, a regra de refugo apareceria em `_build_item_input`, em `accounting/cost.py` e na validação da rota — três lugares para divergir, que é a classe de bug do PR #32.

**Files:**
- Create: `backend/core/quotes/filaments.py`
- Create: `backend/tests/core/test_filaments_rules.py`

**Interfaces:**
- Consumes: `backend.core.quote_service.effective_grams_per_unit`.
- Produces:
  - `grams_for_line(grams_unit: Decimal | None, gcode_meta: dict, density: Decimal, waste_pct: Decimal, diameter_mm: Decimal = Decimal("1.75")) -> Decimal`
  - `waste_for_line(grams_unit: Decimal | None, is_multi_color: bool, single_pct: Decimal, multi_pct: Decimal) -> Decimal`
  - `validate_lines(lines: Sequence[LineShape]) -> None` — levanta `ValueError` com mensagem em português
  - `LineShape` — `Protocol` com `grams_unit: Decimal | None` e `position: int`
  - `renumber(positions: Sequence[int]) -> list[int]` — devolve 1..N preservando a ordem

- [ ] **Step 1: Escrever os testes (vão falhar: módulo não existe)**

Criar `backend/tests/core/test_filaments_rules.py`:

```python
"""Regras puras das linhas de filamento.

Sem banco de propósito: refugo e invariantes precisam de UM lugar só, e esse
lugar precisa ser testável em milissegundos.
"""
from dataclasses import dataclass
from decimal import Decimal

import pytest

from backend.core.quotes.filaments import (
    grams_for_line,
    renumber,
    validate_lines,
    waste_for_line,
)


@dataclass
class Linha:
    grams_unit: Decimal | None
    position: int


DENSIDADE = Decimal("1.24")
META = {"filament_m": 10, "time_s": 3600}


def test_gramas_digitadas_sao_valor_final_e_ignoram_refugo():
    # 20% de refugo não deve encostar num valor digitado (spec 3.3)
    assert grams_for_line(Decimal("12.34"), META, DENSIDADE, Decimal("20")) == Decimal("12.34")


def test_gramas_nulas_derivam_do_gcode_e_aplicam_refugo():
    sem_refugo = grams_for_line(None, META, DENSIDADE, Decimal("0"))
    com_refugo = grams_for_line(None, META, DENSIDADE, Decimal("20"))
    assert sem_refugo > 0
    assert com_refugo == sem_refugo * Decimal("120") / Decimal("100")


def test_gramas_nulas_respeitam_filament_g_do_gcode():
    # filament_g no gcode_meta já é valor final — mesmo contrato de hoje
    meta = {"filament_m": 10, "time_s": 3600, "filament_g": 50}
    assert grams_for_line(None, meta, DENSIDADE, Decimal("20")) == Decimal("50")


def test_refugo_de_linha_digitada_e_zero_mesmo_em_item_multicor():
    assert waste_for_line(Decimal("12.34"), True, Decimal("2"), Decimal("20")) == Decimal("0")


def test_refugo_de_linha_nula_segue_is_multi_color():
    # É isto que mantém o custo histórico idêntico após a 0034: item antigo
    # marcado is_multi_color continua com os 20%.
    assert waste_for_line(None, True, Decimal("2"), Decimal("20")) == Decimal("20")
    assert waste_for_line(None, False, Decimal("2"), Decimal("20")) == Decimal("2")


def test_lista_vazia_e_rejeitada():
    with pytest.raises(ValueError, match="pelo menos um filamento"):
        validate_lines([])


def test_duas_linhas_sem_gramas_sao_rejeitadas():
    # Sem esta regra as duas cairiam no ramo de derivação do gcode e o
    # filamento do item inteiro seria contado duas vezes.
    with pytest.raises(ValueError, match="gramas"):
        validate_lines([Linha(None, 1), Linha(None, 2)])


def test_duas_linhas_com_uma_sem_gramas_sao_rejeitadas():
    with pytest.raises(ValueError, match="gramas"):
        validate_lines([Linha(Decimal("10"), 1), Linha(None, 2)])


def test_duas_linhas_com_gramas_passam():
    validate_lines([Linha(Decimal("10"), 1), Linha(Decimal("5"), 2)])


def test_uma_linha_sem_gramas_passa():
    validate_lines([Linha(None, 1)])


def test_posicoes_nao_contiguas_sao_rejeitadas():
    with pytest.raises(ValueError, match="posição"):
        validate_lines([Linha(Decimal("10"), 1), Linha(Decimal("5"), 3)])


def test_posicoes_duplicadas_sao_rejeitadas():
    with pytest.raises(ValueError, match="posição"):
        validate_lines([Linha(Decimal("10"), 1), Linha(Decimal("5"), 1)])


def test_renumber_preserva_a_ordem():
    assert renumber([5, 1, 9]) == [1, 2, 3]
    assert renumber([]) == []
    assert renumber([1]) == [1]
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker compose run --rm api pytest backend/tests/core/test_filaments_rules.py -q
```

Esperado: `ModuleNotFoundError: No module named 'backend.core.quotes.filaments'`.

- [ ] **Step 3: Implementar o módulo**

Criar `backend/core/quotes/__init__.py` vazio se o pacote ainda não existir, e `backend/core/quotes/filaments.py`:

```python
"""Regras das linhas de filamento de um item.

Puro, sem I/O. Existe para que a regra de refugo e as invariantes do conjunto
de linhas tenham um lugar só — espalhá-las entre o cálculo de pricing, o de
contábil e a validação da rota criaria três versões da mesma regra, que é como
o CPV e o pricing divergiram antes do PR #32.
"""
from __future__ import annotations

from decimal import Decimal
from typing import Protocol, Sequence

from backend.core.quote_service import effective_grams_per_unit

_DIAMETER_MM = Decimal("1.75")


class LineShape(Protocol):
    """O mínimo que uma linha precisa expor para ser validada.

    Protocol em vez de uma classe concreta porque a validação roda tanto sobre o
    payload da API (Pydantic) quanto sobre as linhas já persistidas (ORM).
    """

    grams_unit: Decimal | None
    position: int


def waste_for_line(
    grams_unit: Decimal | None,
    is_multi_color: bool,
    single_pct: Decimal,
    multi_pct: Decimal,
) -> Decimal:
    """Refugo aplicável a uma linha.

    Gramas digitadas são valor final: o número é o que saiu da bobina, purga
    inclusa, exatamente como ``filament_g`` já se comporta. Refugo zero.

    Gramas nulas derivam do gcode e mantêm a regra de hoje, governada por
    ``is_multi_color``. Isto NÃO é concessão a código legado — é o que garante
    que o custo de todo orçamento histórico fique idêntico após a migração
    0034. Trocar para "tem mais de uma linha" reprecificaria itens antigos.
    """
    if grams_unit is not None:
        return Decimal(0)
    return (multi_pct if is_multi_color else single_pct) or Decimal(0)


def grams_for_line(
    grams_unit: Decimal | None,
    gcode_meta: dict,
    density: Decimal,
    waste_pct: Decimal,
    diameter_mm: Decimal = _DIAMETER_MM,
) -> Decimal:
    """Gramas **por peça** de uma linha.

    A multiplicação por ``quantity`` não acontece aqui: ela vive num lugar só,
    no cálculo de custo.
    """
    if grams_unit is not None:
        return grams_unit
    meters = float(gcode_meta.get("filament_m") or 0)
    raw_g = gcode_meta.get("filament_g")
    filament_g = float(raw_g) if raw_g not in (None, "") else None
    return effective_grams_per_unit(meters, filament_g, density, diameter_mm, waste_pct)


def validate_lines(lines: Sequence[LineShape]) -> None:
    """Invariantes do conjunto. Levanta ``ValueError`` na primeira violação.

    São do conjunto, não da linha — e é por isso que a API troca a lista
    inteira em vez de ter um endpoint por linha.
    """
    if not lines:
        raise ValueError("o item precisa de pelo menos um filamento")

    posicoes = [ln.position for ln in lines]
    if sorted(posicoes) != list(range(1, len(lines) + 1)):
        raise ValueError(
            f"posição das linhas precisa ser 1..{len(lines)} sem repetir; veio {sorted(posicoes)}"
        )

    if len(lines) > 1 and any(ln.grams_unit is None for ln in lines):
        raise ValueError(
            "item com mais de um filamento precisa das gramas de cada linha — "
            "não há como dividir o filamento do gcode entre as cores"
        )


def renumber(positions: Sequence[int]) -> list[int]:
    """1..N preservando a ordem de entrada. Usado após remover uma linha."""
    return list(range(1, len(positions) + 1))
```

- [ ] **Step 4: Rodar e confirmar que passa**

```bash
docker compose run --rm api pytest backend/tests/core/test_filaments_rules.py -q
```

Esperado: **14 passed**.

- [ ] **Step 5: Lint e commit**

```bash
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/core/quotes/filaments.py backend/tests/core/test_filaments_rules.py
git add backend/core/quotes/ backend/tests/core/test_filaments_rules.py
git commit -m "feat(multicor): regras puras das linhas de filamento

Refugo e invariantes num lugar só. Gramas digitadas são valor final;
gramas nulas mantêm a regra de hoje governada por is_multi_color, que é o
que preserva o custo histórico após a 0034."
```

---

## Task 4: `ItemInput.filaments` — pricing com N filamentos

**Files:**
- Modify: `backend/core/pricing/quote.py:14-43`
- Modify: `backend/core/quote_service.py:28-62`
- Test: `backend/tests/core/test_pricing_quote.py`

**Interfaces:**
- Consumes: nada da Task 3 (este é puro pricing).
- Produces:
  - `FilamentLine(grams: Decimal, price_per_kg: Decimal)` — dataclass frozen
  - `ItemInput.filaments: tuple[FilamentLine, ...]` **substituindo** `grams` e `price_per_kg`
  - `gcode_to_item_input(..., filaments: tuple[FilamentLine, ...] | None = None)` — quando `None`, monta uma linha a partir de `meta`/`filament_g`/`waste_pct`, como hoje

`grams` e `price_per_kg` **saem** de `ItemInput`. Não ficam como alias: dois jeitos de dizer a mesma coisa é como o CPV divergiu do pricing. `ItemInput(` só é construído em `quote_service.py:52` e em testes — a troca é contida.

- [ ] **Step 1: Escrever o teste (vai falhar)**

Acrescentar em `backend/tests/core/test_pricing_quote.py`:

```python
def test_custo_soma_os_filamentos_e_conta_o_tempo_uma_vez():
    """A armadilha central do multicor.

    Gramas somam por linha de cor; time_s NÃO. O gcode descreve a impressão da
    peça inteira, com todas as suas cores — somar o tempo por linha
    multiplicaria energia e depreciação pelo número de cores.

    Duas cores E quantidade 3 de propósito: com uma cor não dá para distinguir
    as duas regras, e com quantidade 1 o ×quantity fica invisível.
    """
    from backend.core.pricing.cost import depreciation_cost, energy_cost, filament_cost
    from backend.core.pricing.quote import FilamentLine, ItemInput, compute_item_cost

    time_s = 3600
    qty = 3
    power_w = Decimal("150")
    kwh = Decimal("0.95")
    dep_h = Decimal("1.50")

    item = ItemInput(
        filaments=(
            FilamentLine(grams=Decimal("12"), price_per_kg=Decimal("100")),
            FilamentLine(grams=Decimal("8"), price_per_kg=Decimal("250")),  # preço diferente
        ),
        time_s=time_s,
        power_w=power_w,
        kwh_price=kwh,
        depreciation_per_hour=dep_h,
        failure_pct=Decimal("0"),
        quantity=qty,
    )

    fil_esperado = (filament_cost(Decimal("12"), Decimal("100"))
                    + filament_cost(Decimal("8"), Decimal("250")))
    en_esperado = energy_cost(time_s, power_w, kwh)        # UMA vez, não duas
    dep_esperado = depreciation_cost(time_s, dep_h)        # UMA vez, não duas
    esperado = (fil_esperado + en_esperado + dep_esperado) * Decimal(qty)

    assert compute_item_cost(item) == esperado


def test_precos_diferentes_por_linha_nao_viram_media():
    from backend.core.pricing.quote import FilamentLine, ItemInput, compute_item_cost

    base = dict(time_s=0, power_w=Decimal("0"), kwh_price=Decimal("0"),
                depreciation_per_hour=Decimal("0"), failure_pct=Decimal("0"), quantity=1)
    caro_e_barato = compute_item_cost(ItemInput(filaments=(
        FilamentLine(Decimal("10"), Decimal("50")),
        FilamentLine(Decimal("10"), Decimal("450")),
    ), **base))
    # Se alguém "simplificasse" usando a média (250), daria o mesmo número —
    # então o teste usa gramas IGUAIS e preços diferentes de propósito, e
    # compara com a soma explícita, não com a média.
    assert caro_e_barato == Decimal("0.50") + Decimal("4.50")


def test_item_de_uma_linha_custa_o_mesmo_que_antes():
    """Regressão: a troca de grams/price_per_kg por filaments não muda o
    número de um item de uma cor."""
    from backend.core.pricing.quote import FilamentLine, ItemInput, compute_item_cost

    item = ItemInput(
        filaments=(FilamentLine(grams=Decimal("60"), price_per_kg=Decimal("120")),),
        time_s=4 * 3600, power_w=Decimal("150"), kwh_price=Decimal("0.95"),
        depreciation_per_hour=Decimal("1.50"), failure_pct=Decimal("0"), quantity=4,
    )
    assert compute_item_cost(item) == Decimal("55.08")
```

O `55.08` vem do PR #32, que mediu exatamente esse item. Se a troca mudar o número, este teste diz.

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker compose run --rm api pytest backend/tests/core/test_pricing_quote.py -q
```

Esperado: `TypeError` — `ItemInput` não aceita `filaments`.

- [ ] **Step 3: Trocar `ItemInput` e `compute_item_cost`**

Em `backend/core/pricing/quote.py`, substituir as linhas 14-43:

```python
@dataclass(frozen=True)
class FilamentLine:
    """Um filamento de um item: gramas **por peça** e preço do seu material.

    Preço por linha, não por item, porque duas cores podem custar diferente —
    é justamente isso que torna a separação útil.
    """

    grams: Decimal
    price_per_kg: Decimal


@dataclass(frozen=True)
class ItemInput:
    # Um item tem N filamentos. Item de uma cor tem uma linha — não há caminho
    # separado para "cor única", porque dois caminhos divergem.
    filaments: tuple[FilamentLine, ...]
    time_s: float
    power_w: Decimal
    kwh_price: Decimal
    depreciation_per_hour: Decimal
    failure_pct: Decimal
    quantity: int
    # Optional — defaults to 0 so existing call sites that don't pass it
    # keep behaving exactly as before.
    maintenance_per_hour: Decimal = Decimal("0")


@dataclass(frozen=True)
class ServiceLine:
    quantity: Decimal
    rate: Decimal
    is_material: bool  # purga = True; labor/other = False


def compute_item_cost(item: ItemInput) -> Decimal:
    # Gramas somam por linha de filamento; time_s NÃO. O gcode descreve a
    # impressão da peça inteira, com todas as suas cores, então energia,
    # depreciação e manutenção contam o tempo UMA vez — e só depois tudo
    # escala pela quantidade de cópias.
    fil = sum(
        (filament_cost(f.grams, f.price_per_kg) for f in item.filaments), Decimal(0)
    )
    en = energy_cost(item.time_s, item.power_w, item.kwh_price)
    dep = depreciation_cost(item.time_s, item.depreciation_per_hour)
    maint = maintenance_cost(item.time_s, item.maintenance_per_hour)
    base = fil + en + dep + maint
    with_failure = apply_failure(base, item.failure_pct)
    return with_failure * Decimal(item.quantity)
```

- [ ] **Step 4: Atualizar `gcode_to_item_input`**

Em `backend/core/quote_service.py`, trocar a construção do `ItemInput` (linhas ~52-62) e a assinatura:

```python
def gcode_to_item_input(
    meta: GcodeMeta,
    density: Decimal,
    price_per_kg: Decimal,
    power_w: Decimal,
    kwh_price: Decimal,
    depreciation_per_hour: Decimal,
    failure_pct: Decimal,
    quantity: int,
    diameter_mm: Decimal = Decimal("1.75"),
    maintenance_per_hour: Decimal = Decimal("0"),
    waste_pct: Decimal = Decimal("0"),
    filament_g: float | None = None,
    filaments: tuple[FilamentLine, ...] | None = None,
) -> ItemInput:
    """Build an ItemInput for cost computation from parsed gcode metadata and material/settings.

    ``filaments`` explícito vence: é o caminho multicor, já resolvido pelo
    chamador (gramas e preço de cada linha). Quando é ``None``, monta uma linha
    única a partir de ``meta``/``filament_g``/``waste_pct`` — o comportamento de
    sempre, para itens de uma cor.

    ``waste_pct`` inflates the consumed filament to account for purges,
    brims, supports and color-change towers. The caller picks the right
    side of the material's single/multi-color presets based on the item's
    ``is_multi_color`` flag.

    If ``filament_g`` is provided and > 0, it is used as-is (no waste applied).
    """
    if filaments is None:
        grams = effective_grams_per_unit(
            meta.filament_m, filament_g, density, diameter_mm, waste_pct
        )
        filaments = (FilamentLine(grams=grams, price_per_kg=price_per_kg),)
    return ItemInput(
        filaments=filaments,
        time_s=meta.time_s,
        power_w=power_w,
        kwh_price=kwh_price,
        depreciation_per_hour=depreciation_per_hour,
        failure_pct=failure_pct,
        quantity=quantity,
        maintenance_per_hour=maintenance_per_hour,
    )
```

Acrescentar `FilamentLine` ao import existente: `from backend.core.pricing.quote import FilamentLine, ItemInput`.

- [ ] **Step 5: Rodar os testes de pricing**

```bash
docker compose run --rm api pytest backend/tests/core/test_pricing_quote.py -q
```

Esperado: os 3 novos passam. **Se algum teste antigo falhar por construir `ItemInput(grams=…, price_per_kg=…)`**, atualizá-lo para `filaments=(FilamentLine(grams=…, price_per_kg=…),)` — é a mesma asserção com a construção nova, não um afrouxamento.

- [ ] **Step 6: Suíte inteira**

```bash
docker compose run --rm api pytest backend/tests -q
```

Esperado: tudo verde. `test_cpv_concorda_com_o_pricing_no_mesmo_item` constrói `ItemInput` e precisa da mesma atualização.

- [ ] **Step 7: Lint e commit**

```bash
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/core/pricing/quote.py backend/core/quote_service.py \
  backend/tests/core/test_pricing_quote.py
git add backend/core/pricing/quote.py backend/core/quote_service.py \
        backend/tests/core/test_pricing_quote.py backend/tests/core/test_accounting_cost.py
git commit -m "feat(multicor): ItemInput.filaments soma N filamentos

Gramas somam por linha; time_s conta uma vez e só depois escala pela
quantidade. grams/price_per_kg saem de ItemInput em vez de ficarem como
alias — dois jeitos de dizer a mesma coisa é como o CPV divergiu do
pricing antes do PR #32."
```

---

## Task 5: `_build_item_input` lê as linhas do banco

**Files:**
- Modify: `backend/api/routes/quotes/_shared.py:67-101` (`_build_item_input`)
- Modify: `backend/api/routes/quotes/_shared.py:284-310` (`_assert_materials_resolved`)
- Test: `backend/tests/api/test_item_filaments_api.py` (criado na Task 7; aqui só o teste de custo via HTTP)

**Interfaces:**
- Consumes: `QuoteItemFilament` (Task 1), `grams_for_line`/`waste_for_line` (Task 3), `FilamentLine` (Task 4).
- Produces: `_build_item_input` passa a devolver `ItemInput` com N linhas. `_filaments_map(session, item_ids) -> dict[UUID, list[tuple[QuoteItemFilament, MaterialVersion]]]` — em lote, para não reintroduzir N+1.

- [ ] **Step 1: Escrever o teste de custo via HTTP**

Criar `backend/tests/api/test_item_filaments_api.py` com o primeiro teste:

```python
"""Custo e contrato das linhas de filamento, pela API.

Pelo HTTP de propósito: é onde o N+1 e a serialização aparecem, e é o que a
tela consome.
"""
from decimal import Decimal

import pytest
import sqlalchemy as sa

from backend.core.models import QuoteKind, QuoteStatus
from backend.infra.db import session as session_module
from backend.infra.db.models import MaterialVersion, Quote, QuoteItem, QuoteItemFilament, User


async def _material(nome: str, preco: str, cor: str) -> MaterialVersion:
    async with session_module.SessionFactory() as s:
        mv = MaterialVersion(
            material_type="PLA", name=nome, color=cor, manufacturer="ACME",
            density_g_cm3=Decimal("1.24"), price_per_kg_ref=Decimal(preco),
        )
        s.add(mv)
        await s.commit()
        await s.refresh(mv)
        return mv


async def _quote_com_item_multicor(mv_a: MaterialVersion, mv_b: MaterialVersion,
                                   quantity: int) -> Quote:
    async with session_module.SessionFactory() as s:
        u = (await s.execute(sa.select(User))).scalars().first()
        q = Quote(kind=QuoteKind.COMMERCIAL.value, user_id=u.id,
                  status=QuoteStatus.DRAFT.value, markup_pct=Decimal("0"),
                  min_charge=Decimal("0"))
        s.add(q)
        await s.commit()
        it = QuoteItem(quote_id=q.id, name="peça bicolor",
                       gcode_meta={"filament_m": 10, "time_s": 3600},
                       material_version_id=mv_a.id, quantity=quantity)
        s.add(it)
        await s.commit()
        s.add_all([
            QuoteItemFilament(quote_item_id=it.id, material_version_id=mv_a.id,
                              grams_unit=Decimal("12"), position=1),
            QuoteItemFilament(quote_item_id=it.id, material_version_id=mv_b.id,
                              grams_unit=Decimal("8"), position=2),
        ])
        await s.commit()
        await s.refresh(q)
        return q


@pytest.mark.asyncio
async def test_subtotal_soma_as_duas_cores_e_conta_o_tempo_uma_vez(auth_client):
    from backend.core.pricing.cost import depreciation_cost, energy_cost, filament_cost

    mv_a = await _material("PLA Preto", "100", "Preto")
    mv_b = await _material("PLA Dourado", "250", "Dourado")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=3)

    r = await auth_client.get(f"/quotes/{q.id}")
    assert r.status_code == 200
    item = r.json()["items"][0]

    # As duas linhas aparecem, ordenadas por position
    assert [f["grams_unit"] for f in item["filaments"]] == ["12.00", "8.00"]

    # E o subtotal soma o filamento das duas, com o tempo contado uma vez
    async with session_module.SessionFactory() as s:
        st = (await s.execute(sa.text(
            "SELECT printer_power_w, energy_kwh_price, printer_depreciation_per_hour "
            "FROM settings WHERE id = 1"
        ))).one()
    fil = filament_cost(Decimal("12"), Decimal("100")) + filament_cost(Decimal("8"), Decimal("250"))
    en = energy_cost(3600, st[0], st[1])
    dep = depreciation_cost(3600, st[2])
    esperado = (fil + en + dep) * Decimal(3)

    assert Decimal(item["subtotal"]) == esperado.quantize(Decimal("0.01")), (
        "subtotal divergiu — conferir se energia/depreciação foram somadas por cor"
    )
```

Se `failure_rate_pct` ou `printer_maintenance_per_hour` não forem zero no banco de teste, ajustar o esperado incluindo-os; o ponto da asserção é a soma do filamento e o tempo contado uma vez.

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker compose run --rm api pytest backend/tests/api/test_item_filaments_api.py -q
```

Esperado: `KeyError: 'filaments'` — `QuoteItemOut` ainda não expõe o campo.

- [ ] **Step 3: Adicionar `_filaments_map` em `_shared.py`**

```python
async def _filaments_map(
    session: AsyncSession, item_ids: list[UUID]
) -> dict[UUID, list[tuple[QuoteItemFilament, MaterialVersion]]]:
    """Linhas de todos os itens numa query só.

    Buscar por item reintroduziria o N+1 que a Spec 2 acabou de tirar do
    contábil — e aqui seria pior, porque o cálculo de custo percorre os itens.
    """
    if not item_ids:
        return {}
    rows = (
        await session.execute(
            select(QuoteItemFilament, MaterialVersion)
            .join(MaterialVersion, MaterialVersion.id == QuoteItemFilament.material_version_id)
            .where(QuoteItemFilament.quote_item_id.in_(item_ids))
            .order_by(QuoteItemFilament.quote_item_id, QuoteItemFilament.position)
        )
    ).all()
    out: dict[UUID, list[tuple[QuoteItemFilament, MaterialVersion]]] = {}
    for fil, mv in rows:
        out.setdefault(fil.quote_item_id, []).append((fil, mv))
    return out
```

Acrescentar `QuoteItemFilament` ao import de modelos no topo do arquivo.

- [ ] **Step 4: `_build_item_input` monta as linhas**

Substituir o corpo de `_build_item_input`, aceitando as linhas já carregadas:

```python
async def _build_item_input(
    session: AsyncSession,
    it: QuoteItem,
    settings_row: Settings,
    filaments: list[tuple[QuoteItemFilament, MaterialVersion]] | None = None,
) -> ItemInput | None:
    """Return ItemInput for cost calc, or None if the item has no resolved material (pending).

    ``filaments`` vem de ``_filaments_map`` quando o chamador já carregou tudo
    em lote. Quando é ``None``, busca as linhas deste item — caminho usado por
    chamadores de um item só.
    """
    if filaments is None:
        filaments = (await _filaments_map(session, [it.id])).get(it.id, [])

    if not filaments:
        # Item sem linha = material não resolvido (pendente). Mesma semântica
        # que o `if not it.material_version_id` de antes.
        return None

    deprec = it.depreciation_rate_override or settings_row.printer_depreciation_per_hour
    # failure_pct sai do material da PRIMEIRA linha: é uma propriedade da
    # impressão, não de cada cor, e a linha 1 é o material principal do item.
    primeiro_mv = filaments[0][1]
    failure = it.failure_rate_override or primeiro_mv.failure_rate_pct

    linhas: list[FilamentLine] = []
    for fil, mv in filaments:
        waste = waste_for_line(
            fil.grams_unit,
            bool(it.is_multi_color),
            mv.single_color_waste_pct,
            mv.multi_color_waste_pct,
        )
        gramas = grams_for_line(fil.grams_unit, it.gcode_meta, mv.density_g_cm3, waste)
        linhas.append(FilamentLine(grams=gramas, price_per_kg=mv.price_per_kg_ref))

    meta = GcodeMeta(
        time_s=float(it.gcode_meta.get("time_s") or 0),
        filament_m=float(it.gcode_meta.get("filament_m") or 0),
        material=it.gcode_meta.get("material"),
        machine=it.gcode_meta.get("machine"),
    )
    return gcode_to_item_input(
        meta=meta,
        density=primeiro_mv.density_g_cm3,
        price_per_kg=primeiro_mv.price_per_kg_ref,
        power_w=settings_row.printer_power_w,
        kwh_price=settings_row.energy_kwh_price,
        depreciation_per_hour=deprec,
        failure_pct=failure,
        quantity=it.quantity,
        maintenance_per_hour=settings_row.printer_maintenance_per_hour or Decimal("0"),
        filaments=tuple(linhas),
    )
```

`density` e `price_per_kg` seguem na chamada porque a assinatura os exige, mas são ignorados quando `filaments` é explícito. Imports novos: `from backend.core.quotes.filaments import grams_for_line, waste_for_line` e `FilamentLine`.

- [ ] **Step 5: `_quote_out` expõe `filaments` e usa o lote**

Em `_quote_out`, ao lado de `consumptions_by_item`:

```python
    filaments_by_item = await _filaments_map(session, [it.id for it in items])
```

e no `QuoteItemOut(...)`:

```python
            filaments=[
                QuoteItemFilamentOut(
                    id=str(fil.id),
                    material_id=str(mv.id),
                    material_name=mv.name,
                    material_color=mv.color,
                    grams_unit=fil.grams_unit,
                    position=fil.position,
                )
                for fil, mv in filaments_by_item.get(it.id, [])
            ],
```

Onde `_build_item_input` for chamado dentro de `_quote_out`, passar `filaments=filaments_by_item.get(it.id, [])` para não refazer a query.

- [ ] **Step 6: `_assert_materials_resolved` olha as linhas**

Trocar a checagem de `it.material_version_id` por "o item tem ao menos uma linha", usando `_filaments_map` em lote. A mensagem de erro existente (`"Resolva N peça(s) com material pendente antes de finalizar"`) fica igual — é a que a tela mostra e um teste e2e depende dela.

- [ ] **Step 7: Rodar**

```bash
docker compose run --rm api pytest backend/tests/api/test_item_filaments_api.py -q
docker compose run --rm api pytest backend/tests -q
```

- [ ] **Step 8: Conferir que não voltou N+1**

`backend/tests/api/test_sales_query_count.py` tem o padrão de contagem de queries. Acrescentar um teste análogo: um orçamento com 3 itens, cada um com 2 linhas, e a contagem de queries do `GET /quotes/{id}` não cresce em relação a um orçamento com 1 item de 1 linha mais um número fixo pequeno.

- [ ] **Step 9: Lint e commit**

```bash
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/api/routes/quotes/_shared.py backend/tests/api/test_item_filaments_api.py
git add backend/api/routes/quotes/_shared.py backend/tests/api/test_item_filaments_api.py
git commit -m "feat(multicor): custo do orçamento lê as linhas de filamento

_filaments_map carrega tudo em lote — buscar por item reintroduziria o
N+1 que a Spec 2 tirou do contábil."
```

---

## Task 6: CPV do contábil soma as linhas

**Importante antes de começar:** o CPV **não** usa `catalog_filament`. O teste existente `test_compute_quote_costs_components` afirma `costs.cpv == real_filament + energy + depreciation + services` — o filamento do CPV vem do **consumo real** (`material_consumptions`), que já é 1:N. Logo multicor **não muda a fórmula do CPV**; muda o número de referência `catalog_filament`, que é o estimado.

Isso é uma simplificação real, não um atalho: energia e depreciação já foram corrigidas no PR #32 e não mudam aqui (o tempo continua contado uma vez por item).

**Files:**
- Modify: `backend/core/accounting/cost.py:58-105`
- Test: `backend/tests/core/test_accounting_cost.py`

**Interfaces:**
- Consumes: `QuoteItemFilament` (Task 1), `grams_for_line`/`waste_for_line` (Task 3).
- Produces: nada novo. `compute_quote_costs` passa a somar `catalog_filament` sobre as linhas.

- [ ] **Step 1: Escrever o teste (vai falhar)**

Acrescentar em `backend/tests/core/test_accounting_cost.py`:

```python
@pytest.mark.asyncio
async def test_catalog_filament_soma_as_cores_e_energia_conta_uma_vez():
    """Item bicolor: filamento de catálogo soma as duas linhas; energia e
    depreciação continuam contando o time_s uma vez (× quantidade).

    Duas cores E quantidade 2, porque com uma cor ou com quantidade 1 as duas
    regras ficam indistinguíveis.
    """
    from backend.infra.db.models import QuoteItemFilament

    async with session_module.SessionFactory() as s:
        user = User(name="u", email="multicor@t.com", password_hash="x")
        mv_a = MaterialVersion(material_type="PLA", name="PLA Preto", color="Preto",
                               density_g_cm3=Decimal("1.24"), price_per_kg_ref=Decimal("100"))
        mv_b = MaterialVersion(material_type="PLA", name="PLA Ouro", color="Ouro",
                               density_g_cm3=Decimal("1.24"), price_per_kg_ref=Decimal("300"))
        settings = await s.merge(Settings(id=1, energy_kwh_price=Decimal("1.00"),
                                         printer_power_w=Decimal("100"),
                                         printer_depreciation_per_hour=Decimal("2.00")))
        s.add_all([user, mv_a, mv_b]); await s.commit()
        q = Quote(kind=QuoteKind.COMMERCIAL.value, user_id=user.id,
                  status=QuoteStatus.PRODUZIDO.value, markup_pct=Decimal("0"),
                  min_charge=Decimal("0"))
        s.add(q); await s.commit()
        item = QuoteItem(quote_id=q.id, name="bicolor",
                         gcode_meta={"filament_m": 10, "time_s": 3600},
                         material_version_id=mv_a.id, quantity=2)
        s.add(item); await s.commit()
        s.add_all([
            QuoteItemFilament(quote_item_id=item.id, material_version_id=mv_a.id,
                              grams_unit=Decimal("30"), position=1),
            QuoteItemFilament(quote_item_id=item.id, material_version_id=mv_b.id,
                              grams_unit=Decimal("10"), position=2),
        ])
        await s.commit()

        costs = await compute_quote_costs(s, q, settings)

        # filamento: (30g × R$100/kg) + (10g × R$300/kg) = 3,00 + 3,00 = 6,00, × 2 cópias
        assert costs.catalog_filament == Decimal("12.00"), (
            f"catalog_filament {costs.catalog_filament} — conferir se as duas linhas somaram"
        )
        # energia: 100W × 1h ÷ 1000 × R$1 = 0,10 → × 2 cópias = 0,20. NÃO × 2 cores.
        assert costs.energy == Decimal("0.20"), (
            f"energia {costs.energy} — se deu 0.40, o time_s foi somado por cor"
        )
        # depreciação: 1h × R$2,00 = 2,00 → × 2 cópias = 4,00. NÃO × 2 cores.
        assert costs.depreciation == Decimal("4.00"), (
            f"depreciacao {costs.depreciation} — se deu 8.00, o time_s foi somado por cor"
        )


@pytest.mark.asyncio
async def test_item_de_uma_linha_nula_mantem_o_custo_de_antes_do_backfill():
    """O custo de um item não muda quando o backfill cria sua linha.

    É a asserção que prova que a migração 0034 não mexe em dinheiro. A suíte
    roda as migrações antes de haver dados, então o teste faz o inverso do que
    a spec descreve: calcula o custo do item SEM linha, cria a linha pelo mesmo
    SQL do backfill, e calcula de novo.
    """
    import importlib
    import sqlalchemy as sa

    mig = importlib.import_module("migrations.versions.0034_quote_item_filaments")

    async with session_module.SessionFactory() as s:
        user = User(name="u", email="backfillcost@t.com", password_hash="x")
        mv = MaterialVersion(material_type="PLA", name="PLA", color="Preto",
                             density_g_cm3=Decimal("1.24"), price_per_kg_ref=Decimal("100"),
                             single_color_waste_pct=Decimal("2"),
                             multi_color_waste_pct=Decimal("20"))
        settings = await s.merge(Settings(id=1, energy_kwh_price=Decimal("1.00"),
                                         printer_power_w=Decimal("100"),
                                         printer_depreciation_per_hour=Decimal("2.00")))
        s.add_all([user, mv]); await s.commit()
        q = Quote(kind=QuoteKind.COMMERCIAL.value, user_id=user.id,
                  status=QuoteStatus.PRODUZIDO.value, markup_pct=Decimal("0"),
                  min_charge=Decimal("0"))
        s.add(q); await s.commit()
        # is_multi_color=True de propósito: é o caso em que a regra de refugo
        # poderia "simplificar" errado e reprecificar o histórico.
        item = QuoteItem(quote_id=q.id, name="antigo",
                         gcode_meta={"filament_m": 10, "time_s": 3600},
                         material_version_id=mv.id, quantity=2, is_multi_color=True)
        s.add(item); await s.commit()

        antes = await compute_quote_costs(s, q, settings)
        await s.execute(sa.text(mig.BACKFILL_SQL))
        await s.commit()
        s.expire_all()
        depois = await compute_quote_costs(s, q, settings)

    assert depois.catalog_filament == antes.catalog_filament, (
        "o backfill mudou o filamento de catálogo — a regra de refugo da linha "
        "NULL deve seguir is_multi_color, não a contagem de linhas"
    )
    assert depois.cpv == antes.cpv
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
docker compose run --rm api pytest backend/tests/core/test_accounting_cost.py -q
```

Esperado: o primeiro teste falha com `catalog_filament` errado (só a linha 1 contada, ou o material do item em vez das linhas). O segundo pode já passar — se passar, confirmar **por que** antes de seguir: com `grams_unit NULL` a derivação é a mesma, então passar é o resultado correto, e ele vira guarda de regressão.

- [ ] **Step 3: Somar as linhas em `compute_quote_costs`**

Substituir, dentro do `for it in items:`, o bloco que calcula `grams_unit`/`catalog_filament`:

```python
    # Linhas de filamento de todos os itens numa query só.
    filaments_by_item: dict = {}
    if items:
        rows = (
            await session.execute(
                select(QuoteItemFilament, MaterialVersion)
                .join(MaterialVersion, MaterialVersion.id == QuoteItemFilament.material_version_id)
                .where(QuoteItemFilament.quote_item_id.in_([it.id for it in items]))
                .order_by(QuoteItemFilament.quote_item_id, QuoteItemFilament.position)
            )
        ).all()
        for fil, mv_line in rows:
            filaments_by_item.setdefault(fil.quote_item_id, []).append((fil, mv_line))

    for it in items:
        linhas = filaments_by_item.get(it.id, [])
        if not linhas:
            # Item sem linha = material não resolvido. Mesma semântica que o
            # `if mv is None: continue` de antes.
            continue
        time_s = float(it.gcode_meta.get("time_s", 0))
        qty = Decimal(it.quantity)

        # Gramas somam por linha de cor; time_s NÃO — ele descreve a impressão
        # da peça inteira, com todas as suas cores.
        for fil, mv_line in linhas:
            waste = waste_for_line(
                fil.grams_unit, bool(it.is_multi_color),
                mv_line.single_color_waste_pct, mv_line.multi_color_waste_pct,
            )
            gramas_unit = grams_for_line(
                fil.grams_unit, it.gcode_meta, mv_line.density_g_cm3, waste
            )
            catalog_filament += filament_cost(gramas_unit * qty, mv_line.price_per_kg_ref)

        # (o comentário do PR #32 sobre quantity segue válido e fica aqui)
        energy += energy_cost(time_s, settings_row.printer_power_w, settings_row.energy_kwh_price) * qty
        dep_rate = it.depreciation_rate_override or settings_row.printer_depreciation_per_hour
        depreciation += depreciation_cost(time_s, dep_rate) * qty
```

**Atenção:** o cálculo antigo passava `Decimal("0")` como `waste_pct`, ou seja o contábil nunca aplicava refugo. Agora `waste_for_line` aplica. Isso **muda** `catalog_filament` de itens com refugo cadastrado — e é correto, porque alinha com o pricing. Mas `catalog_filament` **não entra no CPV**, então o DRE não muda. Confirmar isso rodando `test_dashboard_aligned_with_dre_v2`; se ele quebrar, a premissa está errada e é preciso parar e reavaliar, não ajustar o teste.

O bloco de `real_filament` (consumo real) fica **inalterado**.

- [ ] **Step 4: Rodar**

```bash
docker compose run --rm api pytest backend/tests/core/test_accounting_cost.py -q
docker compose run --rm api pytest backend/tests -q
```

- [ ] **Step 5: Lint e commit**

```bash
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/core/accounting/cost.py backend/tests/core/test_accounting_cost.py
git add backend/core/accounting/cost.py backend/tests/core/test_accounting_cost.py
git commit -m "feat(multicor): catalog_filament soma as linhas de filamento

CPV não muda: seu filamento vem do consumo real, que já é 1:N. Energia e
depreciação seguem contando o time_s uma vez por item (PR #32)."
```

---

## Task 7: Contrato de API — substituição da lista

**Files:**
- Modify: `backend/api/schemas/quotes.py`
- Modify: `backend/api/routes/quotes/items.py:32-174`
- Test: `backend/tests/api/test_item_filaments_api.py`

**Interfaces:**
- Consumes: `validate_lines`/`renumber` (Task 3), `_filaments_map` (Task 5).
- Produces:
  - `QuoteItemFilamentOut(id: str, material_id: str, material_name: str, material_color: str | None, grams_unit: Decimal | None, position: int)`
  - `QuoteItemFilamentIn(material_id: str, grams_unit: Decimal | None = None)` — sem `position`: a ordem da lista **é** a posição, então não há como o cliente enviar posição inconsistente
  - `QuoteItemOut.filaments: list[QuoteItemFilamentOut] = []`
  - `QuoteItemUpdate.filaments: list[QuoteItemFilamentIn] | None = None`

- [ ] **Step 1: Escrever os testes de contrato**

Acrescentar em `backend/tests/api/test_item_filaments_api.py`:

```python
@pytest.mark.asyncio
async def test_filaments_e_material_id_no_mesmo_patch_e_400(auth_client):
    mv_a = await _material("PLA A", "100", "A-400")
    mv_b = await _material("PLA B", "100", "B-400")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=1)
    r0 = await auth_client.get(f"/quotes/{q.id}")
    item_id = r0.json()["items"][0]["id"]

    r = await auth_client.put(
        f"/quotes/{q.id}/items/{item_id}",
        json={"material_id": str(mv_b.id),
              "filaments": [{"material_id": str(mv_a.id), "grams_unit": "10"}]},
    )
    assert r.status_code == 400
    assert "material_id" in r.text


@pytest.mark.asyncio
async def test_patch_sem_filaments_preserva_as_linhas(auth_client):
    """A regressão óbvia é tratar None como lista vazia e apagar tudo."""
    mv_a = await _material("PLA A", "100", "A-401")
    mv_b = await _material("PLA B", "100", "B-401")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=1)
    r0 = await auth_client.get(f"/quotes/{q.id}")
    item_id = r0.json()["items"][0]["id"]

    r = await auth_client.put(f"/quotes/{q.id}/items/{item_id}", json={"name": "novo nome"})
    assert r.status_code == 200
    item = [i for i in r.json()["items"] if i["id"] == item_id][0]
    assert item["name"] == "novo nome"
    assert len(item["filaments"]) == 2, "o PATCH apagou as linhas"


@pytest.mark.asyncio
async def test_material_id_sozinho_reescreve_a_linha_1_sem_duplicar(auth_client):
    mv_a = await _material("PLA A", "100", "A-402")
    mv_b = await _material("PLA B", "100", "B-402")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=1)
    r0 = await auth_client.get(f"/quotes/{q.id}")
    item_id = r0.json()["items"][0]["id"]
    mv_c = await _material("PLA C", "100", "C-402")

    r = await auth_client.put(f"/quotes/{q.id}/items/{item_id}",
                              json={"material_id": str(mv_c.id)})
    assert r.status_code == 200
    item = [i for i in r.json()["items"] if i["id"] == item_id][0]
    assert len(item["filaments"]) == 2
    assert item["filaments"][0]["material_id"] == str(mv_c.id)
    assert item["material_id"] == str(mv_c.id), "o derivado não acompanhou a linha 1"


@pytest.mark.asyncio
async def test_lista_vazia_e_rejeitada(auth_client):
    mv_a = await _material("PLA A", "100", "A-403")
    mv_b = await _material("PLA B", "100", "B-403")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=1)
    r0 = await auth_client.get(f"/quotes/{q.id}")
    item_id = r0.json()["items"][0]["id"]

    r = await auth_client.put(f"/quotes/{q.id}/items/{item_id}", json={"filaments": []})
    assert r.status_code == 400
    assert "pelo menos um filamento" in r.text


@pytest.mark.asyncio
async def test_duas_linhas_sem_gramas_sao_rejeitadas(auth_client):
    mv_a = await _material("PLA A", "100", "A-404")
    mv_b = await _material("PLA B", "100", "B-404")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=1)
    r0 = await auth_client.get(f"/quotes/{q.id}")
    item_id = r0.json()["items"][0]["id"]

    r = await auth_client.put(
        f"/quotes/{q.id}/items/{item_id}",
        json={"filaments": [{"material_id": str(mv_a.id)},
                            {"material_id": str(mv_b.id)}]},
    )
    assert r.status_code == 400
    assert "gramas" in r.text


@pytest.mark.asyncio
async def test_remover_a_linha_do_meio_renumera(auth_client):
    mv_a = await _material("PLA A", "100", "A-405")
    mv_b = await _material("PLA B", "100", "B-405")
    mv_c = await _material("PLA C", "100", "C-405")
    q = await _quote_com_item_multicor(mv_a, mv_b, quantity=1)
    r0 = await auth_client.get(f"/quotes/{q.id}")
    item_id = r0.json()["items"][0]["id"]

    # três linhas, depois remove a do meio mandando a lista sem ela
    await auth_client.put(f"/quotes/{q.id}/items/{item_id}", json={"filaments": [
        {"material_id": str(mv_a.id), "grams_unit": "10"},
        {"material_id": str(mv_b.id), "grams_unit": "5"},
        {"material_id": str(mv_c.id), "grams_unit": "2"},
    ]})
    r = await auth_client.put(f"/quotes/{q.id}/items/{item_id}", json={"filaments": [
        {"material_id": str(mv_a.id), "grams_unit": "10"},
        {"material_id": str(mv_c.id), "grams_unit": "2"},
    ]})
    assert r.status_code == 200
    item = [i for i in r.json()["items"] if i["id"] == item_id][0]
    assert [f["position"] for f in item["filaments"]] == [1, 2]
    assert [f["material_id"] for f in item["filaments"]] == [str(mv_a.id), str(mv_c.id)]


@pytest.mark.asyncio
async def test_item_novo_ja_nasce_com_a_linha_1(auth_client):
    """add_item resolve o material e precisa criar a linha, senão o item nasce
    violando a invariante 1 e não orça."""
    mv = await _material("PLA Novo", "100", "Novo-406")
    # usar o mesmo caminho que a tela usa para adicionar item manual
    # (conferir a assinatura real de POST /quotes/{id}/items em items.py:32)
    ...
```

O último teste precisa da forma real do `POST /quotes/{id}/items` — ler `backend/api/routes/quotes/items.py:32-95` e escrever a chamada que a tela faz. Não deixar como `...`.

- [ ] **Step 2: Rodar e confirmar que falham**

```bash
docker compose run --rm api pytest backend/tests/api/test_item_filaments_api.py -q
```

- [ ] **Step 3: Schemas**

Em `backend/api/schemas/quotes.py`, antes de `QuoteItemOut`:

```python
class QuoteItemFilamentOut(BaseModel):
    id: str
    material_id: str
    material_name: str
    material_color: str | None = None
    # Gramas POR PEÇA. None = "derive do gcode_meta", que é o caso de todo item
    # anterior à migração 0034 — a tela mostra o derivado em cinza.
    grams_unit: Decimal | None = None
    position: int


class QuoteItemFilamentIn(BaseModel):
    material_id: str
    grams_unit: Decimal | None = None
    # Sem `position`: a ORDEM DA LISTA é a posição. Aceitar posição do cliente
    # abriria espaço para um conjunto inconsistente que o servidor teria de
    # rejeitar — mais um caminho de erro por nada.
```

Em `QuoteItemOut`, ao lado de `consumptions`:

```python
    # Filamentos orçados — um por cor. Ordenados por position.
    filaments: list[QuoteItemFilamentOut] = []
```

Em `QuoteItemUpdate`:

```python
    # Substituição da lista inteira. None = não mexer (um PATCH que só muda o
    # nome não deve apagar as linhas). As invariantes são do CONJUNTO, e por
    # isso não há endpoint por linha: validar um conjunto em três endpoints
    # seria três lugares para a mesma regra divergir.
    filaments: list[QuoteItemFilamentIn] | None = None
```

Em `ConsumptionOut`, para a Task 8:

```python
    # Qual linha de filamento originou este consumo. None = consumo anterior à
    # 0034, ou baixa feita sem identificar a cor.
    filament_id: str | None = None
```

- [ ] **Step 4: `update_item` substitui a lista**

Em `backend/api/routes/quotes/items.py`, dentro de `update_item`, antes de aplicar os outros campos:

```python
    if payload.filaments is not None and payload.material_id is not None:
        raise HTTPException(
            400,
            "envie material_id OU filaments, não os dois — material_id é o "
            "atalho para um item de uma cor e reescreve a linha 1",
        )

    if payload.filaments is not None:
        novas = []
        for pos, linha in enumerate(payload.filaments, start=1):
            mv = await session.get(MaterialVersion, UUID(linha.material_id))
            if mv is None:
                raise HTTPException(400, f"material {linha.material_id} não existe")
            novas.append(
                QuoteItemFilament(
                    quote_item_id=it.id,
                    material_version_id=mv.id,
                    grams_unit=linha.grams_unit,
                    position=pos,
                )
            )
        try:
            validate_lines(novas)
        except ValueError as exc:
            raise HTTPException(400, str(exc)) from exc

        # Apagar e recriar, não fazer diff: a UNIQUE (quote_item_id, position)
        # faz um update em ordem colidir consigo mesmo no meio do caminho, e a
        # substituição é o contrato declarado.
        await session.execute(
            delete(QuoteItemFilament).where(QuoteItemFilament.quote_item_id == it.id)
        )
        await session.flush()
        session.add_all(novas)
        await session.flush()
        # Derivado (invariante 2)
        it.material_version_id = novas[0].material_version_id
```

E no caminho de `material_id` sozinho, depois de resolver `mv`, atualizar **a linha 1** e o derivado:

```python
        linha1 = (
            await session.execute(
                select(QuoteItemFilament)
                .where(QuoteItemFilament.quote_item_id == it.id)
                .where(QuoteItemFilament.position == 1)
            )
        ).scalar_one_or_none()
        if linha1 is None:
            session.add(QuoteItemFilament(quote_item_id=it.id, material_version_id=mv.id,
                                          grams_unit=None, position=1))
        else:
            linha1.material_version_id = mv.id
        it.material_version_id = mv.id
```

- [ ] **Step 5: `add_item` cria a linha 1**

Onde `add_item` resolve o material e grava `it.material_version_id`, criar também a linha 1 com `grams_unit=None`. Item que entra pendente (sem material) não ganha linha — e é por isso que a invariante 1 fala em "item com material resolvido".

- [ ] **Step 6: Rodar, lint, commit**

```bash
docker compose run --rm api pytest backend/tests/api/test_item_filaments_api.py -q
docker compose run --rm api pytest backend/tests -q
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/api/schemas/quotes.py backend/api/routes/quotes/items.py \
  backend/tests/api/test_item_filaments_api.py
git add backend/api/schemas/quotes.py backend/api/routes/quotes/items.py \
        backend/tests/api/test_item_filaments_api.py
git commit -m "feat(multicor): API das linhas por substituição da lista

As invariantes são do conjunto, então o PATCH recebe o conjunto. None
preserva as linhas; filaments+material_id juntos é 400."
```

---

## Task 8: Produção

**Files:**
- Modify: `backend/api/routes/quotes/transitions.py:256-310` (`apply_production`)
- Modify: `backend/api/schemas/quotes.py` (`ConsumptionAssignment`)
- Modify: `backend/api/routes/quotes/_shared.py` (`_consumptions_map` expõe `filament_id`)
- Create: `backend/tests/api/test_produce_multicor.py`

**Interfaces:**
- Consumes: `QuoteItemFilament`, `Spool.material_version_id`.
- Produces: `ConsumptionAssignment.quote_item_filament_id: str | None = None`.

- [ ] **Step 1: Escrever os testes**

Criar `backend/tests/api/test_produce_multicor.py` com três testes:

1. **`test_produzir_multicor_sem_identificar_a_linha_e_409_e_nao_debita`** — o teste que importa. Item com 2 linhas, dois assignments sem `quote_item_filament_id` e sem `grams`. Esperar 409 **e** que `remaining_grams` das duas bobinas esteja **inalterado**. A asserção sobre o estoque é o ponto: um 409 sem conferir o débito não prova que nada foi debitado antes de falhar.

```python
    saldo_antes = [sp_a.remaining_grams, sp_b.remaining_grams]
    r = await auth_client.post(f"/quotes/{q.id}/transitions/produce", json={
        "consumption": [
            {"quote_item_id": item_id, "spool_id": str(sp_a.id)},
            {"quote_item_id": item_id, "spool_id": str(sp_b.id)},
        ]
    })
    assert r.status_code == 409
    assert "filamento" in r.text.lower()
    async with session_module.SessionFactory() as s:
        depois = [
            (await s.get(Spool, sp_a.id)).remaining_grams,
            (await s.get(Spool, sp_b.id)).remaining_grams,
        ]
    assert depois == saldo_antes, "debitou estoque antes de rejeitar"
```

2. **`test_cada_linha_debita_da_sua_bobina_com_a_densidade_do_seu_material`** — dois assignments **com** `quote_item_filament_id`, materiais de densidades diferentes, e conferir que cada bobina foi debitada pelas gramas da sua linha × quantidade, e que cada `MaterialConsumption` gravou o `quote_item_filament_id` correspondente.

3. **`test_bobina_de_material_divergente_avisa_e_conclui`** — bobina cujo `material_version_id` difere do material da linha: a produção **conclui** (200) e o aviso aparece. Definir onde o aviso sai antes de escrever a asserção: um campo `warnings: list[str]` no `QuoteOut` de resposta é o caminho mais simples e testável; se preferir só `logger.warning`, o teste usa `caplog`. **Escolher um e escrever a asserção concreta** — "avisar" sem lugar definido é um placeholder.

- [ ] **Step 2: Rodar e confirmar que falham**

- [ ] **Step 3: Campo no schema**

```python
class ConsumptionAssignment(BaseModel):
    quote_item_id: str
    spool_id: str
    # Qual linha de filamento esta baixa representa. Obrigatório quando o item
    # tem mais de uma linha: sem isso o fallback calcularia as gramas do item
    # INTEIRO para cada linha, debitando N× o item.
    quote_item_filament_id: str | None = None
    grams: Decimal | None = None
    filament_m: float | None = None
```

- [ ] **Step 4: Corrigir `apply_production`**

Dentro do `for assign in assignments:`, substituir a resolução de material e o fallback:

```python
        linhas = (
            await session.execute(
                select(QuoteItemFilament)
                .where(QuoteItemFilament.quote_item_id == it.id)
                .order_by(QuoteItemFilament.position)
            )
        ).scalars().all()

        linha = None
        if assign.quote_item_filament_id:
            linha = next(
                (ln for ln in linhas if str(ln.id) == assign.quote_item_filament_id), None
            )
            if linha is None:
                raise HTTPException(
                    400, f"filamento {assign.quote_item_filament_id} não pertence a este item"
                )
        elif len(linhas) > 1:
            raise HTTPException(
                409,
                f"item '{it.name}' tem {len(linhas)} filamentos — informe "
                "quote_item_filament_id em cada baixa. Sem isso, cada linha "
                "debitaria as gramas do item inteiro.",
            )
        else:
            linha = linhas[0] if linhas else None

        if linha is None:
            raise HTTPException(409, f"item '{it.name}' não tem filamento resolvido")

        # Densidade e material saem da LINHA, não do item: com multicor cada
        # cor tem a sua.
        mv = await session.get(MaterialVersion, linha.material_version_id)

        if assign.grams is not None and assign.grams > 0:
            grams = assign.grams
        elif linha.grams_unit is not None:
            grams = linha.grams_unit * Decimal(it.quantity)
        else:
            if assign.filament_m is not None and assign.filament_m > 0:
                meta = dict(it.gcode_meta or {})
                meta["filament_m"] = float(assign.filament_m)
                it.gcode_meta = meta
            grams = grams_for_item(it.gcode_meta, mv.density_g_cm3, it.quantity)
```

E no `MaterialConsumption(...)` criado adiante, acrescentar `quote_item_filament_id=linha.id`.

O aviso de material divergente vai depois de resolver `sp` e `mv`:

```python
        if sp.material_version_id and sp.material_version_id != linha.material_version_id:
            # Aviso, não bloqueio: o backfill da 0035 deixa material_version_id
            # NULL em parte das bobinas, e bloquear por dado incompleto
            # impediria produzir.
            logger.warning(
                "bobina %s é de outro material que a linha %s do item %s",
                sp.id, linha.id, it.id,
            )
```

Carregar as linhas **fora** do laço se houver mais de um assignment para o mesmo item — a query por assignment é aceitável para 2-3 cores, mas se o executor preferir, um `_filaments_map` antes do laço evita repetição.

- [ ] **Step 5: `_consumptions_map` expõe `filament_id`**

Acrescentar `filament_id=str(cons.quote_item_filament_id) if cons.quote_item_filament_id else None` ao `ConsumptionOut`.

- [ ] **Step 6: Rodar, lint, commit**

```bash
docker compose run --rm api pytest backend/tests/api/test_produce_multicor.py -q
docker compose run --rm api pytest backend/tests -q
docker compose run --rm api ruff check --select E4,E7,E9,F \
  backend/api/routes/quotes/transitions.py backend/api/schemas/quotes.py \
  backend/api/routes/quotes/_shared.py backend/tests/api/test_produce_multicor.py
git commit -am "feat(multicor): produção debita por linha de filamento

Densidade vem da linha, não do item. Item com N linhas exige
quote_item_filament_id em cada baixa — sem isso o fallback calcularia as
gramas do item inteiro para cada linha e debitaria N× o item."
```

---

## Task 9: PDF

**Files:**
- Modify: `backend/infra/pdf/templates/quote.html:16-22`
- Test: `backend/tests/infra/test_pdf_render.py`

**Interfaces:**
- Consumes: `QuoteItemOut.filaments` (Task 7). O contexto do template precisa expor as linhas — conferir como `it` chega ao template em `backend/infra/pdf/` e acrescentar `filaments` ao dicionário.

- [ ] **Step 1: Escrever os testes**

Em `backend/tests/infra/test_pdf_render.py`:

1. **`test_pdf_de_uma_cor_nao_mudou`** — renderizar um item de uma linha e afirmar que a saída **não** contém marcador de lista nem repetição de material. Ancorar na string real do material (`"PLA Preto"` aparece uma vez).
2. **`test_pdf_multicor_lista_as_cores`** — item com três linhas: os três nomes aparecem, e as gramas de cada um.
3. **`test_retail_mode_lista_cores_sem_gramas`** — os nomes aparecem, os números de gramas não.

- [ ] **Step 2: Rodar e confirmar que falham**

```bash
docker compose run --rm api pytest backend/tests/infra/test_pdf_render.py -q
```

- [ ] **Step 3: Alterar o macro**

```jinja
{% macro material_label(it) %}
  {%- if it.filaments and it.filaments|length > 1 -%}
    {%- for f in it.filaments -%}
      {{ f.material_name }}{% if f.material_color %} · {{ f.material_color }}{% endif %}
      {%- if not retail_mode and f.grams_unit %} · {{ "%.2f"|format(f.grams_unit|float) }} g{% endif %}
      {%- if not loop.last %}<br>{% endif -%}
    {%- endfor -%}
  {%- elif it.material_name -%}
    {{ it.material_name }}
    {%- if it.material_color %} · {{ it.material_color }}{%- endif -%}
  {%- elif it.material_polymer -%}
    {{ it.material_polymer }}{%- if it.is_multi_color %} · multicolor{%- endif -%}
  {%- endif -%}
{% endmacro %}
```

Item de uma linha cai no ramo `elif it.material_name` — **idêntico ao de hoje**, sem lista e sem `<br>`. É o caso majoritário e o PDF não deve mudar por causa de uma feature que ele não usa.

Conferir se `retail_mode` está no escopo do macro; em Jinja, variáveis globais do contexto estão, mas se o macro estiver definido antes do `set`, passar `retail_mode` como argumento.

- [ ] **Step 4: Rodar, lint, commit**

---

## Task 10: Tela

**Files:**
- Create: `frontend/src/lib/filament-lines.ts`
- Create: `frontend/src/lib/filament-lines.test.ts`
- Create: `frontend/src/lib/components/FilamentLines.svelte`
- Modify: `frontend/src/routes/quotes/[id]/+page.svelte`

**Interfaces:**
- Produces:
  - `sumGrams(lines: Line[]): number | null` — `null` quando alguma linha tem `grams_unit` nulo (não há soma parcial honesta)
  - `gcodeGrams(gcodeMeta: Record<string, unknown>, density: number): number` — o derivado, para mostrar em cinza
  - `delta(soma: number | null, gcode: number): number | null`
  - `withoutIndex(lines: Line[], i: number): Line[]` — remove e renumera

- [ ] **Step 1: Testes Vitest da lógica pura**

```ts
import { describe, expect, it } from "vitest";
import { delta, sumGrams, withoutIndex } from "./filament-lines";

describe("sumGrams", () => {
  it("soma as gramas das linhas", () => {
    expect(sumGrams([{ grams_unit: 12.34 }, { grams_unit: 5.66 }])).toBe(18);
  });

  it("devolve null quando alguma linha não tem gramas", () => {
    // Não existe soma parcial honesta: mostrar 12,34 quando falta uma linha
    // sugere que o total é 12,34.
    expect(sumGrams([{ grams_unit: 12.34 }, { grams_unit: null }])).toBeNull();
  });

  it("devolve null para lista vazia, não 0", () => {
    expect(sumGrams([])).toBeNull();
  });
});

describe("delta", () => {
  it("é a diferença entre a soma e o gcode", () => {
    expect(delta(18.9, 18.9)).toBe(0);
    expect(delta(20, 18.9)).toBeCloseTo(1.1);
  });

  it("é null quando a soma é null", () => {
    expect(delta(null, 18.9)).toBeNull();
  });
});

describe("withoutIndex", () => {
  it("remove e renumera", () => {
    const linhas = [
      { position: 1, material_id: "a" },
      { position: 2, material_id: "b" },
      { position: 3, material_id: "c" },
    ];
    expect(withoutIndex(linhas, 1)).toEqual([
      { position: 1, material_id: "a" },
      { position: 2, material_id: "c" },
    ]);
  });

  it("não remove a última linha", () => {
    const uma = [{ position: 1, material_id: "a" }];
    expect(withoutIndex(uma, 0)).toEqual(uma);
  });
});
```

- [ ] **Step 2: Rodar e confirmar que falham**

```bash
cd frontend && npx vitest run src/lib/filament-lines.test.ts
```

- [ ] **Step 3: Implementar `filament-lines.ts`**

- [ ] **Step 4: Rodar e confirmar que passam**

- [ ] **Step 5: `FilamentLines.svelte`**

Layout da seção 8 da spec. Pontos que a spec fixa e que o componente deve respeitar:

- linha com `grams_unit` nulo mostra o derivado do gcode **em cinza**, editável; digitar preenche
- botão `×` desabilitado quando há só uma linha
- o rodapé mostra `soma · gcode · delta`, e **nunca** impede salvar
- `+ cor` acrescenta uma linha; ao passar de uma para duas linhas, a tela precisa exigir gramas nas duas (invariante 3) — sinalizar no próprio campo, não só no erro do servidor

**Reatividade Svelte — armadilha conhecida deste projeto:** a descoberta de dependências de um bloco `$:` é uma **varredura textual**. Variável lida dentro de uma função chamada é invisível, e o bloco não re-roda. Já causou três bugs aqui (chips de status que não filtravam, `perdaPeriodo` que não atualizava). Passar os valores como **argumentos**, para que apareçam textualmente e sejam de fato usados:

```svelte
$: soma = sumGrams(linhas);
$: diferenca = delta(soma, gcodeTotal);
```

e não `$: diferenca = calcular();` lendo `linhas` por dentro.

- [ ] **Step 6: Ligar em `+page.svelte`**

Substituir o seletor único de material pelo componente, e enviar `filaments` no PUT do item. Enviar `filaments` **e** `material_id` no mesmo payload é 400 (Task 7) — remover o `material_id` do payload quando as linhas forem enviadas.

- [ ] **Step 7: `npm run check` e `npx vitest run`**

```bash
cd frontend && npm run check && npx vitest run
```

Erros pré-existentes em `library`/`spools` não contam; nenhum erro **novo**.

- [ ] **Step 8: Commit**

---

## Self-review deste plano

**Cobertura da spec**, seção por seção:

| spec | task |
|---|---|
| 3.1 abordagem C (derivado) | 1 (modelo), 7 (manutenção do derivado + teste) |
| 3.2 gramas digitadas | 7 (API), 10 (tela) |
| 3.3 valor final sem refugo | 3 (`waste_for_line`) |
| 3.4 `grams_unit` nullable | 1 (backfill), 6 (teste de custo antes/depois) |
| 3.5 delta sem bloqueio | 10 |
| 4 modelo + invariantes | 1 (tabela), 3 (validação), 7 (aplicação) |
| 4.1 migração 0034 | 1 |
| 4.2 migração 0035 | 2 |
| 4.3 FK do consumo | 1 (coluna), 8 (gravação e exposição) |
| 5 custo | 4 (pricing puro), 5 (pricing com banco), 6 (contábil) |
| 6 produção | 8 |
| 7 medallion | **nenhuma task** — é outro repositório, por desenho. Entregue como lista na spec |
| 8 tela | 10 |
| 9 API e PDF | 7, 9 |

**Lacunas conhecidas e assumidas:**

- **Seção 7 (medallion) não tem task** neste plano, e isso é deliberado: é outro repositório e a abordagem C garante que ele siga funcionando. Precisa de plano próprio lá.
- **Task 7, Step 1, último teste** (`test_item_novo_ja_nasce_com_a_linha_1`) está com `...` porque depende da assinatura real de `POST /quotes/{id}/items`, que o executor precisa ler. É a única lacuna deliberada do plano, e está marcada como "não deixar como `...`".
- **Task 8, Step 1, teste 3**: o lugar do aviso de material divergente (campo `warnings` na resposta vs `logger.warning` + `caplog`) está como escolha do executor, com a exigência de escrever a asserção concreta. Escolher `logger.warning` é o caminho mais barato e não muda contrato.

**Consistência de tipos** — conferido: `FilamentLine(grams, price_per_kg)` (Task 4) é o que `_build_item_input` constrói (Task 5); `grams_for_line`/`waste_for_line` (Task 3) têm a mesma assinatura nos três chamadores (Tasks 5, 6); `QuoteItemFilamentOut.grams_unit` é `Decimal | None` no schema e `number | null` no TS; `quote_item_filament_id` é `str | None` no schema e `UUID | None` no modelo.

**Contagem de testes esperada ao fim:** 305 (atual) + 3 (T1) + 5 (T2) + 14 (T3) + 3 (T4) + ~2 (T5) + 2 (T6) + 7 (T7) + 3 (T8) + 3 (T9) = **~347** no backend, mais ~8 no Vitest.
